{
  "name": "signal",
  "version": "0.1.0",
  "private": true,
  "description": "SIGNAL — a first-person tower-climbing game. Restore the network, one antenna at a time.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --serve",
    "build": "node scripts/icons.mjs && node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "icons": "node scripts/icons.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "esbuild": "^0.28.2",
    "three": "^0.185.1",
    "typescript": "^5.9.0"
  }
}
